git push origin master

sudo docker-compose run app jekyll build

rsync \
  --archive \
  --verbose \
  --compress \
  --delete \
  --rsh=ssh \
  _site/ \
  benjaminoakes.com:~/jekyll.benjaminoakes.com/
